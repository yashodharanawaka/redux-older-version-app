import React, { Component } from "react";

class LoadFail extends Component {
  render() {
    return (
      <div>
        <p className="lead">Unable to retrieve the drama list.</p>
      </div>
    );
  }
}

export default LoadFail;
